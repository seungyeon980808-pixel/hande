# 이 앱은 무엇을 없애려고 만들었는가

> 기능 목록이 아니라 **왜 만드는가**를 적은 문서입니다.
> 기능을 추가하거나 뺄 때 판단 기준으로 씁니다.

---

## 1. 없애려는 것 — "그 30분"

학교에서 자료를 취합하는 담당자에게는 매번 반복되는 30분이 있습니다.
기능을 나열하기 전에, **정확히 어느 순간의 어떤 불편인지** 먼저 못 박습니다.

### 담당자 쪽에서 벌어지는 일

```
양식 만들기 → 메신저에 올리기 → 안 낸 사람 확인 → 개별 독촉
   → 파일 20개 내려받기 → 20번 열기 → 20번 복사·붙여넣기
      → 이름 다른 파일 정리 → 최종본 하나 만들기
```

여기서 진짜 고통은 **파일을 모으는 일이 아니라 합치는 일**입니다.
파일은 메신저가 이미 모아줍니다. 그런데 그 20개를 **하나의 표로 만드는 건 사람 손**입니다.
셀 위치가 다르고, 시트 이름이 다르고, 누구는 행을 늘렸고, 누구는 서식을 바꿨습니다.

> **복사·붙여넣기 20번이 이 앱이 겨냥하는 정확한 지점입니다.**

### 작성자 쪽에서 벌어지는 일

```
메신저에서 첨부 찾기 → 내려받기 → 한글 실행 → 작성
   → 다른 이름으로 저장 → 파일명을 규칙대로 고치기 → 메신저에 다시 첨부
```

여기서 고통은 작성이 아니라 **작성 앞뒤에 붙은 절차**입니다.
내려받고, 이름 바꾸고, 다시 올리는 그 왕복.
그리고 **"이거 낸 거 맞나?"** 하는 불확실함 — 메신저에는 제출 여부를 보여주는 화면이 없습니다.

### 둘 사이에서 사라지는 것

- 마감이 지났는데 누가 안 냈는지 **스크롤을 올려가며** 찾습니다
- 같은 파일이 세 번 올라와 있고 **뭐가 최신인지** 모릅니다
- 수정본을 받으면 이전 것을 덮어써서 **분쟁 시 증거가 없습니다**
- 취합이 끝난 뒤 그 파일들이 **어디에 얼마나 남아 있는지** 아무도 모릅니다

---

## 2. 그래서 무엇을 하는가

**링크 하나로 이 흐름 전체를 대체합니다.**

담당자가 취합 요청을 만들면 두 개의 주소가 나옵니다.

| 주소 | 받는 사람 | 하는 일 |
|---|---|---|
| **제출 링크** | 전체 대상자 | 열고, 쓰고, 제출 — 그게 끝 |
| **관리 링크** | 담당자 | 현황 확인 + 결과물 한 번에 받기 |

작성자에게는 **내려받기도 없고, 파일명 규칙도 없고, 다시 첨부하기도 없습니다.**
담당자에게는 **복사·붙여넣기가 없습니다.**

### 세 가지 유형 — 같은 무게가 아닙니다

| 유형 | 작성자가 얻는 것 | 담당자가 얻는 것 |
|---|---|---|
| **웹 표** | 브라우저에서 바로 입력 | **버튼 1번 → 통합 엑셀 1장** |
| 한글 문서 | 한글 없이 브라우저에서 작성 | 사람별 파일 (합쳐지지 않음) |
| 엑셀 파일 | 익숙한 Excel 그대로 | ZIP 묶음 (합쳐지지 않음) |

> **웹 표만이 "합치는 일"을 실제로 없앱니다.**
> 나머지 둘은 "모으는 일"을 편하게 할 뿐이고, 그건 메신저도 이미 하고 있습니다.

이 비대칭이 이 앱의 정체성입니다. 세 개를 나란히 놓되, **웹 표가 왜 다른지 화면에서 보이게** 해야 합니다.

---

## 3. 설계 철학 — 왜 이렇게 만들었는가

### 3-1. 마찰 없는 제출이 최우선이다

**계정을 만들지 않습니다.** 로그인도 비밀번호도 없습니다.
링크를 누르면 바로 작성 화면입니다.

이유는 단순합니다. 대상자 30명에게 계정을 발급하는 순간 **메신저보다 번거로워집니다.**
아무리 좋은 기능이 있어도 쓰지 않으면 0입니다. 학교 업무 도구는 여기서 대부분 죽습니다.

> 단, **"마찰이 낮다"와 "누구나 사칭할 수 있다"는 다릅니다.**
> 철학은 지키되 사칭은 막아야 합니다 — 개별 링크처럼 마찰을 늘리지 않는 방법이 있습니다.

### 3-2. 합칠 수 있는 형태로만 받는다

웹 표에서 **작성자는 열을 바꾸지 못하고 행만 추가**합니다.
불편해 보이지만 이게 핵심입니다. 열 구조가 제각각이 되는 순간 **자동 취합은 불가능해지고, 우리는 다시 복사·붙여넣기로 돌아갑니다.**

같은 이유로 **임의 엑셀 파일은 자동 병합하지 않습니다.**
셀 위치가 다른 파일들을 억지로 합치면 **조용히 틀린 데이터**가 만들어집니다.
합칠 수 없는 건 합치는 척하지 않고, 원본을 보존한 채 그대로 전달합니다.

> **절반쯤 맞는 자동화보다, 안 하는 편이 낫습니다.**

### 3-3. 덮어쓰지 않는다

재제출해도 이전 제출을 **지우지 않고 버전으로 쌓습니다.**

학교 업무는 감사의 성격을 가집니다. "그때 뭐라고 냈었나"가 나중에 문제가 됩니다.
최신본만 남기는 시스템은 **분쟁이 생겼을 때 아무것도 증명하지 못합니다.**

### 3-4. 구형 포맷을 표준으로 수렴시킨다

HWP를 받으면 HWPX로 정규화합니다. 되돌리지 않습니다.
당장은 번거로워도, **다음 사람이 다룰 수 있는 형태로 남기는 것**이 맞습니다.

### 3-5. 담당자에게 과한 정보를 주지 않는다

관리 화면은 **"누가 작성 중인지"만** 보여주고 **임시저장 내용은 보여주지 않습니다.**

기술적으로는 보여줄 수 있습니다. 그런데 보여주는 순간
*"임시저장하면 부장이 본다"*는 인식이 생기고, **작성자들은 임시저장 기능 자체를 안 쓰게 됩니다.**
그러면 작성 중 이탈로 내용이 통째로 날아갑니다.

> 할 수 있다고 다 하지 않습니다. **신뢰가 기능보다 먼저입니다.**

### 3-6. 학교 안에서 돈다

담당자 컴퓨터가 서버입니다. 외부 클라우드에 문서가 올라가지 않습니다.
문서는 `public` 폴더 밖에 UUID 이름으로 저장되고, 주소는 256비트 난수 토큰입니다.

> 다만 **"폐쇄망"이라고 말하려면 외부에서 불러오는 것이 하나도 없어야 합니다.**
> 지금은 편집기를 외부에서 로드합니다. 이 문장을 쓰기 전에 내부화가 먼저입니다.

---

## 4. 이 앱이 하지 않는 일

목적이 선명하려면 **안 하는 것**도 선명해야 합니다.

| 안 하는 것 | 이유 |
|---|---|
| 계정·비밀번호 관리 | 마찰이 생기면 아무도 안 씁니다 |
| 임의 엑셀 자동 병합 | 조용히 틀린 데이터를 만듭니다 |
| 문서 뷰어·편집기 자체 개발 | 우리가 잘할 수 있는 영역이 아닙니다 |
| 메신저 대체 | 알림과 대화는 이미 잘 돌아갑니다 |
| 종합 업무 포털 | *"취합앱인데 왜 급식이 있지?"* 가 됩니다 |

**이 앱은 취합 도구입니다.** 그 한 가지를 끝까지 잘하는 것이 목표입니다.

---

## 5. 성공을 무엇으로 판단하는가

기능이 늘었는지가 아니라, **아래 문장이 참이 되었는지**로 판단합니다.

1. 담당자가 취합을 마치는 데 **복사·붙여넣기를 0번** 했다
2. 작성자가 **파일을 내려받지도, 이름을 바꾸지도, 다시 올리지도** 않았다
3. 담당자가 **스크롤을 올려가며 누가 안 냈는지 찾지 않았다**
4. 취합이 끝난 뒤 **그 자료가 어디에 얼마나 남아 있는지 답할 수 있다**

1번과 2번은 현재 구조로 달성됩니다.
3번은 관리 화면이 하고 있습니다.
**4번은 아직 답할 수 없습니다** — 삭제·보존 정책이 없기 때문입니다.

---

## 6. 한 문장으로

> **여러 사람이 각자 쓴 것을, 아무도 손으로 합치지 않아도 하나가 되게 한다.**

기능을 넣을지 말지 고민될 때 이 문장에 비춰봅니다.
이 문장을 더 참으로 만들면 넣고, 그렇지 않으면 넣지 않습니다.
