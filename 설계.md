# SchoolFlow 검토 결과 — 설계 정리

> 2026-08-26 기준 전수 검토 요약. 원본 레포를 clone → 빌드·테스트 → 소스 검토 → 교차 검토한 결과에서 **핵심만** 추렸습니다.
> 상세 근거는 별도 검토 문서에 있습니다.

---

## 0. 검증 결과 — 뼈대는 견고합니다

`npm test` 19개 전부 통과 · 빌드/린트 에러 0 · 페이지 5개 200 OK.

직접 공격해봤으나 **막힌** 것들:

- 경로 탈출 (`"../../etc/passwd"` → `"etc_passwd"`)
- 엑셀 수식 인젝션 (exceljs가 방어)
- 관리 API 12개 전수 확인 — 토큰 검증 누락 없음

토큰은 256비트 난수의 SHA-256 해시만 저장(원문 미보관), 파일은 UUID 이름으로 `public` 밖에 권한 `0o600`, 원자적 쓰기(temp → rename) + 직렬 큐, 마감 이중 검사.
**README의 기술적 주장과 코드가 일치합니다. 과장이 없습니다.**

---

## 1. 우선순위 — 시간 축으로 나눕니다

섞으면 판단이 흐려집니다. **언제 터지는가**로 가릅니다.

| 묶음 | 시점 | 성격 |
|---|---|---|
| **A** | 시연 전 (이틀) | 메시지를 뾰족하게 — 기능 추가 아님 |
| **B** | 실제 학교 데이터 넣기 전 | 보안 필수 묶음 |
| **C** | 운영 단계 | 구조 재설계 |

**A와 B를 섞지 않는 것이 핵심입니다.**

---

## 2. A — 시연 전

### A-1. 웹 표를 전면에 배치 ⭐ 가장 중요

세 유형의 **담당자 작업량**이 완전히 다릅니다.

| 유형 | 담당자 작업 | 근거 |
|---|---|---|
| **웹 표** | **버튼 1번 → 통합 엑셀 1장** | `xlsx.ts`가 전원 rows를 한 시트에 append |
| 엑셀 파일 | ZIP 1번 → **복붙 수작업 그대로** | `download-latest`는 파일 내용을 한 바이트도 안 읽음 |
| 한글 문서 | **일괄 다운로드 없음** | 인원수만큼 클릭 |

**웹 표만이 "여러 스프레드시트를 손으로 합치는 흐름"을 실제로 없앱니다.**
그런데 현재 UI에서 웹 표는 "엑셀 취합" 하위 2단계에 숨어 있고, **결과물이 뭔지 화면 어디에도 없습니다.**

→ 평평한 카드 3장 + 결과물 명시. `request-form.tsx` + `globals.css` 3열 그리드. **서버 무변경, 2~3시간.**

### A-2. 빈 메뉴 2개 제거

`통합 대시보드`·`자료실`이 `href="#"`. 6개 중 2개가 껍데기이고 **`통합 대시보드`가 맨 위**입니다.
심사위원이 가장 먼저 누를 자리 → 채우지 말고 **감추는 편이 낫습니다.** 30분.

### A-3. HWPX 자동 치환 리허설 · `deadline` 기본값

실제 양식으로 반드시 사전 리허설. `deadline` 기본값이 없어 매번 손으로 찍어야 함 → "3일 뒤 17시" 기본값(5분).

---

## 3. B — 실제 데이터 넣기 전 (필수 묶음)

> 교차 검토의 결론: **삭제보다 사칭·무인증이 먼저입니다.**
> 삭제는 목적 달성 *후*의 위험이지만, 사칭과 무제한 업로드는 **서비스가 열린 순간부터의 현재형 위험**입니다.

### B-1. 요청 생성이 무인증 🚨

`page.tsx`·`requests/new/page.tsx` 모두 토큰 검증 없음, 미들웨어 부재.
주소만 알면 **학교망의 누구나**(학생 포함) 전체 취합 현황과 명단을 봅니다.
더 심각한 건 **아무나 학교 명의의 가짜 취합 링크를 발급**할 수 있다는 점 — 피싱 경로가 됩니다.

→ `/`, `/requests/new`, `POST /api/collections`에 권한 검사. **화면만 가리면 안 되고 API 자체에** 필요합니다.

### B-2. 제출자 사칭 🚨

신원 검증의 전부가 `recipients.find(r => r.id === teacherId)`.
`teacherId`가 `t01`~ 형태로 **추측 가능**하고, 제출 페이지가 **전체 명단을 클라이언트로 내려줍니다.**
링크는 단톡방을 거치며 준공개되므로, 링크를 얻은 사람이 남의 이름으로 제출할 수 있습니다. 재제출 제한도 없습니다.

**"링크 클릭 → 바로 작성"이라는 핵심 가치는 지켜야 합니다. 계정·비밀번호 발급은 하지 않습니다.**
다만 *마찰이 낮다*와 *사칭이 가능하다*는 다릅니다.

| 방식 | 마찰 |
|---|---|
| **교사별 개별 링크** ← 유력 | 거의 없음 (경험은 지금과 동일) |
| 이름 선택 + 4자리 확인 코드 | 낮음 |
| 학교 SSO | 중간 (운영 단계) |

### B-3. 무제한 업로드 🚨

rate limit **0건**, 임시저장 개수 상한 없음, 버전 상한 없음, 요청당 총량 제한 없음.
`draftKey`를 계속 바꾸면 같은 사람 아래에 대용량 파일을 무한 생성 가능.
저장소가 **매 요청마다 `state.json` 전체를 읽고 쓰므로**, 디스크가 차면 **기존 제출 데이터까지 손상**될 수 있습니다.

### B-4. 압축 폭탄 🚨

`personalizeHwpx()`가 HWPX를 `unzipSync()`로 메모리에 통째로 해제. 해제 크기·항목 수·개별 XML 크기 제한이 **전부 없음.**
20MB 업로드가 풀면 수 GB가 될 수 있고, **B-1(무인증)과 결합하면 원격 메모리 고갈 경로**가 됩니다.

### B-5. rHWP 외부 의존 ⚠️

라이선스는 문제없음(MIT, 확인 완료). 문제는 **"폐쇄망"이라면서 편집기를 외부에서 로드**한다는 구조적 모순 — 폐쇄망에서 화면이 무한 정지할 수 있습니다. 내부화 + 로딩 타임아웃 필요.

### B-6. 삭제·보존 정책 부재

앱에 제출물을 지우는 기능이 없고, 임시본·버전이 무한 축적됩니다. 목적 달성 후에도 개인정보가 계속 남습니다.

### B-7. 그 외

공용 PC에서 임시본 자동 복구로 **남의 문서가 열림** · URL에 비밀정보 노출 · 마감 후에도 임시저장 허용 · HTTPS/보안 헤더.

---

## 4. C — 운영 단계

### C-1. PostgreSQL은 "어댑터만 만들면" 되는 상태가 아닙니다 ⛔

`src/db/schema.ts`에 웹 표용 컬럼이 통째로 없습니다.

- `collections.type` 없음 → 유형 구분 불가
- 열 정의(`table`) 저장할 곳 없음
- `submission_versions.kind`/`rows` 없음 → **표 제출을 못 담음**
- `storage_key`가 `.notNull()`인데 표 제출은 빈 문자열을 씀

**스키마가 웹 표 기능 추가 전에 만들어지고 갱신되지 않았습니다. 재설계부터 해야 합니다.**
`Collection`이 `recipients[].versions[].rows[]`까지 중첩된 단일 객체라, 관계형으로 풀면 4테이블 조인 + 트랜잭션 재설계입니다.
**여러 명이 작업하는 동안 저장 계층을 바꾸면 나머지 전부가 블로킹됩니다.**

### C-2. 직렬 큐는 단일 프로세스 전제

`createSerialQueue()`가 모듈 스코프 변수라, 프로세스를 여러 개 띄우면 상호배제가 없어 **제출이 조용히 유실**됩니다.
현재 Dockerfile은 단일 프로세스라 안전하지만 **우연히 안전한 것**입니다.

### C-3. 감사 로그가 비어 있음

`auditEvents` 테이블은 정의돼 있으나 **쓰는 코드가 0줄.** "감사 추적 가능"이라고 말하면 안 됩니다.

### C-4. 의존성

취약점 6건(moderate). 미사용 3개 제거 가능 — `archiver`, `@rhwp/core`, `postgres`.

---

## 5. ⛔ 건드리면 안 되는 것 (의도적 설계)

모르고 "개선"하면 핵심이 무너집니다.

| 설계 | 바꾸면 |
|---|---|
| 제출을 덮어쓰지 않고 **버전 축적** | 감사 성격 상실, 기존 다운로드 링크 404 |
| **HWP → HWPX 정규화** | 구형 포맷으로 후퇴 |
| **작성자는 열을 못 바꿈 (행만 추가)** | "여러 명 것을 한 표로"라는 핵심 가치가 즉시 붕괴 |
| **임의 XLSX 자동 병합 안 함** | 절반쯤 되는 병합기는 **조용히 틀린 데이터**를 만듦 |
| 관리 화면이 draft를 안 보여줌 | "임시저장하면 부장이 본다" 인식 → 기능 자체를 안 씀 |
| **마감 이중 검사** | 마감 직후 제출이 통과 |
| `src/lib/*.test.ts` 9개 | 회귀 감지 안전망 상실 |

**손대기 전 `npx vitest run` (0.4초). 빨간불이면 되돌립니다.**

**안전한 영역:** `globals.css` · 죽은 사이드바 링크 · `manuals.ts` · 툴박스 기본 목록 · 대상자 명단
**단기간에 하지 말 것:** PostgreSQL 교체 · 계정 로그인 · 임의 XLSX 병합 · 뷰어 추가 · 급식/학사일정 확장

---

## 6. 남은 질문

1. 나이스 신규 시스템과 기능이 겹치는 지점은 어디까지인가
2. 보존 기간 — 목적 달성 후 며칠 뒤 파기할 것인가
3. 사칭 방지를 **개별 링크**로 갈 것인가, **확인 코드**로 갈 것인가

---

## 7. 협업 시 주의

코드 포맷이 압축돼 있어 **공동 작업 시 diff 충돌 위험**이 큽니다. 작업 시작 전 포맷 규칙을 합의하는 편이 좋습니다.
