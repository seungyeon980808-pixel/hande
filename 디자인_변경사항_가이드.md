# 앱 디자인 변경사항 가이드

이 문서는 다른 버전의 앱(`hande--1`, `hande--2` 등) 폴더에 디자인 변경사항을 동일하게 적용하기 위한 가이드입니다. 

## 1. 사이드바 브랜드 타이틀 변경
**파일 위치:** `src/components/app-frame.tsx`

왼쪽 사이드바의 브랜드 링크 텍스트를 다음과 같이 수정합니다.
(기존 "학교업무 한곳" -> "수합의 정석", "교직원 업무지원" -> "한글·엑셀 파일 자동 취합")

```tsx
<Link href="#" className="brand" style={{ display: "block", textDecoration: "none" }}>
  수합의 정석 <small>한글·엑셀 파일 자동 취합</small>
</Link>
```

### 1-1. 서브 타이틀 폰트 크기 조절
**파일 위치:** `src/app/globals.css`

`.brand small` 의 폰트 크기를 `16px`에서 `14px`로 2px 줄입니다.

```css
.brand small { display:block; margin:4px 0 0; font-size:14px; font-weight:400; opacity:.7; }
```

## 2. 상태별 카드 색상 클래스 추가
**파일 위치:** `src/app/globals.css`

`globals.css` 파일의 `.stats`와 `.stat` 클래스 정의 바로 아래에 상태별 색상 클래스를 추가합니다.

```css
.stat-primary { background:var(--blue-soft); } .stat-primary b, .stat-primary span { color:var(--navy-dark); }
.stat-success { background:var(--success-soft); } .stat-success b, .stat-success span { color:var(--success-text); }
.stat-warning { background:var(--warning-soft); } .stat-warning b, .stat-warning span { color:var(--warning-text); }
.stat-error { background:var(--error-soft); } .stat-error b, .stat-error span { color:var(--error-text); }
```

## 3. 홈 화면 통계 카드 로직 수정
**파일 위치:** `src/app/page.tsx`

하단에 위치한 `Stat` 컴포넌트 함수를 찾아 다음과 같이 수정하여 라벨에 따라 색상이 동적으로 적용되도록 합니다.

```tsx
function Stat({label,value}:{label:string;value:number}){
  let c="card stat";
  if(label==="진행 중 요청") c+=" stat-primary";
  else if(label==="제출 완료") c+=" stat-success";
  else if(label==="마감 임박") c+=" stat-warning";
  else if(label==="미제출") c+=" stat-error";
  return <div className={c}><span>{label}</span><b>{value}</b></div>;
}
```

## 4. 관리자 화면 통계 카드 로직 수정 (해당할 경우)
**파일 위치:** `src/app/manage/[id]/[token]/page.tsx`

동일하게 파일 하단에 위치한 `Stat` 컴포넌트 함수를 찾아 수정합니다.

```tsx
function Stat({label,value}:{label:string;value:number}){
  let c="card stat";
  if(label==="제출 완료") c+=" stat-success";
  else if(label==="작성 중" || label==="마감 임박") c+=" stat-warning";
  else if(label==="미작성" || label==="미제출") c+=" stat-error";
  return <div className={c}><span>{label}</span><b>{value}</b></div>;
}
```
