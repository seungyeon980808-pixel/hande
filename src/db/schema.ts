import { bigint,integer,pgTable,text,timestamp,uuid,varchar } from "drizzle-orm/pg-core";
export const collections=pgTable("collections",{id:uuid("id").primaryKey(),title:varchar("title",{length:80}).notNull(),description:varchar("description",{length:500}).notNull(),deadline:timestamp("deadline",{withTimezone:true}).notNull(),shareTokenHash:varchar("share_token_hash",{length:64}).notNull().unique(),manageTokenHash:varchar("manage_token_hash",{length:64}).notNull(),templateStorageKey:text("template_storage_key").notNull(),templateName:text("template_name").notNull(),templateSize:bigint("template_size",{mode:"number"}).notNull(),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull()});
export const recipients=pgTable("recipients",{id:uuid("id").primaryKey(),collectionId:uuid("collection_id").references(()=>collections.id,{onDelete:"cascade"}).notNull(),teacherKey:varchar("teacher_key",{length:40}).notNull(),name:varchar("name",{length:50}).notNull(),department:varchar("department",{length:80}).notNull()});
export const versions=pgTable("submission_versions",{id:uuid("id").primaryKey(),recipientId:uuid("recipient_id").references(()=>recipients.id,{onDelete:"cascade"}).notNull(),version:integer("version").notNull(),storageKey:text("storage_key").notNull(),displayName:text("display_name").notNull(),size:bigint("size",{mode:"number"}).notNull(),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull()});
export const drafts=pgTable("submission_drafts",{id:uuid("id").primaryKey(),recipientId:uuid("recipient_id").references(()=>recipients.id,{onDelete:"cascade"}).notNull(),deviceKeyHash:varchar("device_key_hash",{length:64}).notNull(),storageKey:text("storage_key").notNull(),displayName:text("display_name").notNull(),size:bigint("size",{mode:"number"}).notNull(),updatedAt:timestamp("updated_at",{withTimezone:true}).defaultNow().notNull()});
export const auditEvents=pgTable("audit_events",{id:uuid("id").primaryKey(),collectionId:uuid("collection_id").references(()=>collections.id,{onDelete:"cascade"}),event:varchar("event",{length:60}).notNull(),detail:text("detail").notNull(),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull()});
